---
name: goodmem:smart
description: Search project memory and get an LLM-synthesized answer
---

Call `smart_search` with the query from $ARGUMENTS. This uses server-side LLM synthesis to generate a natural language answer from relevant memory chunks, rather than returning raw search results. Use this when you want a direct synthesized answer rather than individual document chunks.
